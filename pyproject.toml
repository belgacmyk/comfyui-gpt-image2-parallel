[project]
name = "comfyui-gpt-image2-parallel"
description = "Async/parallel fork of comfyui-gpt-image: OpenAI GPT Image 2.5 Flare/Sunburst and legacy models with your own key. Includes an x2 node (prompt_a/image_a + prompt_b/image_b -> image_a + image_b) that runs both calls concurrently via asyncio.gather."
version = "2.1.0"
license = {file = "LICENSE"}
dependencies = []

[project.urls]
Repository = "https://github.com/belgacmyk/comfyui-gpt-image2-parallel"

[tool.comfy]
PublisherId = "YOUR_PUBLISHER_ID"
DisplayName = "comfyui-gpt-image2-parallel"
Icon = ""
